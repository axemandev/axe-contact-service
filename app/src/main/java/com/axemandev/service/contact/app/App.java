/*
 * This Java source file was generated by the Gradle 'init' task.
 */
package com.axemandev.service.contact.app;

import com.axemandev.service.contact.list.LinkedList;

import static com.axemandev.service.contact.utilities.StringUtils.join;
import static com.axemandev.service.contact.utilities.StringUtils.split;
import static com.axemandev.service.contact.app.MessageUtils.getMessage;

import org.apache.commons.text.WordUtils;

public class App {
    public static void main(String[] args) {
        LinkedList tokens;
        tokens = split(getMessage());
        String result = join(tokens);
        System.out.println(WordUtils.capitalize(result));
    }
}
